{
  "private": true,
  "packageManager": "yarn@4.0.0-rc.26",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspaces foreach run build"
  },
  "devDependencies": {
    "eslint": "latest",
    "typescript": "^4.8.4"
  }
}
